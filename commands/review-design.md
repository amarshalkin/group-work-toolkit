---
description: Ревью готового проекта сессии по чек-листу. Отчёт в формате «светофор» с конкретными фиксами.
argument-hint: [путь к Инструкции модератору и/или Карте результатов, либо сам текст]
allowed-tools: Read, Glob, Grep
---

Пользователь запустил команду `/group-work-toolkit:review-design` с аргументом:

**$ARGUMENTS**

Запусти скилл `review-session-design` из этого же плагина.

Если аргумент — путь к файлу (или нескольким файлам), прочитай их через Read. Если аргумент — цитата текста, работай с ней напрямую. Если аргумент пустой — попроси у пользователя Инструкцию модератору и/или Карту результатов (путь или текст).

Пройди по чек-листу `skills/design-group-work/references/design-review-checklist.md` и выдай отчёт в формате, описанном в SKILL.md скилла `review-session-design`.
