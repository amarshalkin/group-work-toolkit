---
description: Спроектировать групповую работу — от одного такта до многодневного события. Выдаёт Инструкцию модератору и Карту результатов.
argument-hint: [короткое описание сессии: тема, число участников, длительность]
allowed-tools: Read, Write, Edit, Glob, Grep
---

Пользователь запустил команду `/group-work-toolkit:design-session` с аргументом:

**$ARGUMENTS**

Запусти скилл `design-group-work` из этого же плагина. Если аргумент непустой — используй его как первичное описание сессии и сразу переходи к Шагу 1 (уточнение масштаба и глубины методологии через интерактивные вопросы).

Если аргумент пустой — попроси у пользователя короткое описание сессии (тема, примерное число участников, длительность), и только потом переходи к Шагу 1.

Не начинай проектирование без двух обязательных параметров: масштаб и глубина методологии. Оба уточняются через интерактивный механизм вопросов, не в свободном тексте.
